plugins {
    id 'com.android.application'
}

    android {
    namespace 'org.tumegami.geminilogbrowser'
    compileSdk 34

    buildFeatures {
        viewBinding true
    }

    defaultConfig {
      applicationId "org.tumegami.geminilogbrowser"
    minSdk 21
    targetSdk 34
    versionCode 1
    versionName "1.0"

      testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
       release {
           minifyEnabled false
           proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
       }
    }
    }

  dependencies {
    implementation 'com.google.android.material:material:1.12.0'

  }